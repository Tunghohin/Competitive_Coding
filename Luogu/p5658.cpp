#include <iostream>

using namespace std;

int f[500010];
int need[500010];
int matched[500010];

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr), cout.tie(nullptr);

	int n;
	string s;
	cin >> n;
	cin >> s;

	for (int i = 2; i <= n; i++) cin >> f[i];

	int res = 0;
	
}